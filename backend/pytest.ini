[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
# Change these to 'session' so everything shares ONE event loop
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session